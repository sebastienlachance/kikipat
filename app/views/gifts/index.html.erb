
<h2>Entre 0$ et 25$</h2>
<table>
<% @gifts_less_than_25.each do |gift| %>
  <tr>
    <% if gift.reserved.nil? || !gift.reserved   %>
      <td>
        <img src="/assets/accept.png" /> 
      </td>
    <% else %>
      <td>
        <img src="/assets/block.png" /> 
      </td>
    <% end %>
      <td><%= link_to gift.name, gift %></td>
    <td><%= number_to_currency(gift.price, :unit => '$') %></td>
  </tr>
<% end %>
</table>

<br />


<h2>Entre 25$ et 50$</h2>
<table>
<% @gifts_between_25_and_50.each do |gift| %>
  <tr>
    <% if gift.reserved.nil? || !gift.reserved   %>
      <td>
        <img src="/assets/accept.png" /> 
      </td>
    <% else %>
      <td>
        <img src="/assets/block.png" /> 
      </td>
    <% end %>
    <td><%= link_to gift.name, gift %></td>
    <td><%= number_to_currency(gift.price, :unit => '$') %></td>
  </tr>
<% end %>
</table>

<br />

<h2>Entre 50$ et 100$</h2>
<table>
<% @gifts_between_50_and_100.each do |gift| %>
  <tr>
    <% if gift.reserved.nil? || !gift.reserved   %>
      <td>
        <img src="/assets/accept.png" /> 
      </td>
    <% else %>
      <td>
        <img src="/assets/block.png" /> 
      </td>
    <% end %>
    <td><%= link_to gift.name, gift %></td>
    <td><%= number_to_currency(gift.price, :unit => '$') %></td>
  </tr>
<% end %>
</table>

<br />

<h2>100$ et plus</h2>
<table>
<% @gifts_more_than_100.each do |gift| %>
  <tr>
    <% if gift.reserved.nil? || !gift.reserved   %>
      <td>
        <img src="/assets/accept.png" /> 
      </td>
    <% else %>
      <td>
        <img src="/assets/block.png" /> 
      </td>
    <% end %>
    <td><%= link_to gift.name, gift %></td>
    <td><%= number_to_currency(gift.price, :unit => '$') %></td>
  </tr>
<% end %>
</table>

<br />
<% if current_user %>
  <a href="<%= new_gift_path %>">Ajouter une nouvelle idée cadeau</a>
<% end %>
